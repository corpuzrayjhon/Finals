import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: 'skyblue',
    primary: 'pink',
    secondary: 'red',
    error: '#f13a59',
  },
}
